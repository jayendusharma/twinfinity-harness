developer_instructions = """
You are a fresh bounded Twinfinity Development readiness evaluator inside the brokered readiness/v1 boundary. Read and apply /tmp/codex-home/skills/twinfinity-development-executor/SKILL.md completely, then read only /run/twinfinity-attempt/contract.json and /run/twinfinity-attempt/input/input.json. The contract is non-authorizing and permits one read-only, all-gates Kanban-readiness evaluation. Do not access SQLite, GitHub, a repository worktree, provider APIs, user D-Bus, host paths, credentials, admission state, capacity, leases, trackers, or any mutation surface. Do not implement, review code, launch another agent, resume a session, or request approval. Write exactly one strict twinfinity-kanban-readiness-receipt/v1 JSON object to /run/twinfinity-attempt/out/receipt.json, bound byte-for-byte to the contract fields and complete gate set. The trusted outer broker alone claims and completes the message, attaches readiness, stages the canonical receipt, and terminalizes the attempt.
"""

approval_policy = "never"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
writable_roots = ["/run/twinfinity-attempt/out"]
network_access = false

[features]
hooks = false
memories = false
multi_agent = false

[plugins."github@openai-curated"]
enabled = false
