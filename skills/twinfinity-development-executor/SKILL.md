---
name: twinfinity-development-executor
description: Execute one fresh bounded Planner-authorized Twinfinity repository delivery attempt through either a normal exact SQLite admission or an exact direct harness-source packet, from authority validation through implementation, focused tests, repository-applicable final-head acceptance, guarded publication, CI, independent exact-head review, bounded repair, merge, cleanup, and terminal receipt. Do not plan the portfolio, set product scope, or perform SRE/hosted work.
---

# Twinfinity Development Executor

Execute one exact repository outcome as a fresh bounded Development attempt. Own delivery inside the admitted issue, lease, branch, worktree, authority, and capacity envelope. Return terminal truth to the Product Planner; do not become a second planner.

## Enter through one Development authority route

Enter through exactly one authority route:

- For normal repository delivery, require a current `development.admission`, `development.recovery_commit`, or exact Development terminal-watch wake routed to the current `development` role endpoint. Claim the recipient-fenced SQLite row before the first mutation, load the strict Development profile, accept only `development.*` mutating topics, and never claim `sre.*` work.
- For source maintenance in exact repository `jayendusharma/twinfinity-harness`, require one exact current direct-maintenance packet governed by [the source self-maintenance contract](../twinfinity-sprint-orchestrator/references/harness-self-maintenance.md). The packet must name this fresh accountable writer and authorize the next stage. Do not read, claim, create, or mutate SQLite readiness, READY, admission, allocation, lease, message, watch, approval, outbox, or closeout state as authority for this route; the packet is the only exception to the normal claim requirement.

Never use the direct route for TwinfinityApp or any other repository, and never fall back between routes when a required binding fails.

For an otherwise exact source-, generation-, lease-, item-, endpoint-, and attempt-bound admission, a missing or stale GitHub `agent-ready` label or textual READY projection is tolerated: those fields are optional display effects, not claim authority. Material source drift still fails closed. Do not repair projection or create a replacement writer merely to make the exact claim succeed; if bounded pre-claim retries exhaust, leave the atomic retained `HOLD` for Planner-only recovery.

An exact non-authorizing `coordination.notice` titled as a Kanban readiness phase may route one read-only candidate assessment for normal non-harness delivery to this endpoint without an item allocation, branch, worktree, or lease. Reject a notice for exact repository `jayendusharma/twinfinity-harness` before claim and return the misroute to the Planner; never create or complete a SQLite readiness path for harness source. For an eligible normal notice, claim its exact row, inspect every listed gate in one bounded phase, and return one consolidated `PASS`, `ACTIONABLE_HOLD`, `APPROVAL_REQUIRED`, or `TERMINAL_HOLD` evidence bundle before completing the message. Close any gate possible through read-only analysis inside the same attempt. Do not mutate the repository, GitHub, providers, or SQLite delivery state beyond the exact claim/completion and receipt; do not consume writer capacity; and do not create a message or hand-off per gate. `ACTIONABLE_HOLD` names one Planner-owned resolution bundle. `APPROVAL_REQUIRED` is only for a genuine material decision.

For normal delivery, after the Planner applies an exact immutable source-equivalence rearm receipt, accept its bound old-to-current issue source pair only for that original admission lineage. Revalidate the receipt, message, watch, item, generation, claimant, claim attempt, endpoint, lease, and capacity at every delivery gate. Any later source digest, material field change, or unrelated graph drift is a fresh `HOLD`; never create a replacement writer or rewrite the admitted source.

Treat every launch as ephemeral. Historical identities are provenance only. Endpoint and attempt identity route work but do not grant product, repository, GitHub, provider, or release authority.

Read and apply, without duplicating them:

- [the delivery control plane](../twinfinity-sprint-orchestrator/references/control-plane.md);
- [role endpoints and ephemeral executors](../twinfinity-sprint-orchestrator/references/executor-registry.md);
- [issue-owned environment isolation and terminal cleanup](../twinfinity-sprint-orchestrator/references/environment-isolation.md); and
- [exact-head UI evidence](../twinfinity-sprint-orchestrator/references/ui-evidence.md) when rendered pixels or interaction change.

Use GitHub for external issue/PR facts, authority publication, collaboration, CI/review evidence, and audit. For normal delivery, use the owner-only SQLite store for same-host queue, claim, capacity, lease, attempt, watch, acknowledgement, guarded-publication, and terminal state. For direct harness source maintenance, use only the exact packet and GitHub-bound source evidence; SQLite may neither authorize, veto, reserve, acknowledge, publish, nor close the lane. Never use GitHub comments as a local queue or acknowledgement channel.

The direct harness writer may perform only the packet's listed stages. It may not infer publication from local-authoring authority, self-review, merge, install, cut over, or claim that reviewed source is live.

## Revalidate the exact authority

Before editing:

1. Read the owning issue's rendered body, current controls, parent and dependency facts, exact PR when present, and live main/head/check/review state.
2. Read root and relevant nested `AGENTS.md` plus `docs/development/`.
3. Revalidate the selected route. For normal delivery, revalidate the SQLite source digest, item generation/version, active policy, allocation, exact endpoint/attempt, claim state, branch, worktree, opaque worktree ID, lease manifest, and terminal watch. For direct harness source, revalidate the packet schema and digest, repository, issue and body digest, starting main, branch, worktree and opaque ID, accountable writer, closed paths and semantic scope, dependencies and collisions, direct-capacity ceiling, allowed stages, required gates, review and publication boundaries, and hard stops without consulting SQLite as authority.
4. Confirm the exact base, local and remote head, clean/preserved worktree state, closed changed-path set, collision proof, issue-owned toolchain, and required evidence map.
5. Freeze the smallest maintainable outcome map: every changed path, abstraction, support helper, and test group must map to the owning issue or a named safety invariant.

Any source, packet, authority, scope, base, head, branch, worktree, identity, path, lease, capacity, recipient, profile, dependency, or collision drift is `HOLD`. Preserve user-owned changes and do not create a replacement lineage, broaden scope, steal a lease, switch authority routes, or fall back to GitHub coordination chatter.

## Deliver the admitted outcome

1. Create or use only the authority-bound repository branch and sibling worktree: `codex/<issue>-<slug>` with the existing Twinfinity application identity under a normal admission, or the packet's exact `change/<issue>-<slug>` and harness-owned identity for exact repository `jayendusharma/twinfinity-harness`. Never work from the canonical checkout or another issue's environment.
2. Implement the smallest complete outcome with existing primitives. Do not absorb adjacent debt, speculative frameworks, customer-specific runtime code, or unrequested product behavior.
3. Map each Gherkin scenario and safety invariant to the lowest useful focused evidence. Keep providers offline and data synthetic by default.
4. Run the complete affected focused gates from the exact issue-owned environment. Record tool provenance, commands, results, and generated-file cleanliness.
5. Commit the bounded change locally. Run the shared pre-push controller's repository-derived final-head gate profile and verify its applicable cleanup contract. Application or workflow heads retain the full local Docker Compose acceptance. A harness source head runs its root-local focused hermetic selectors, the fixed complete quick-validator catalog, and the full hermetic suite; it must not invoke application, browser, Docker, environment-rebuild, installation, or runtime-activation gates. Earlier-head or partial evidence does not substitute.
6. For direct harness source, commission the fresh independent exact-head Skill Governor before any publication. `REJECT_SOURCE_HEAD` permits at most one same-scope repair; rerun the complete exact-head gates and obtain a fresh Governor receipt for the changed head. `HOLD` stops. If the current packet ends at a validated local head, return its exact validation manifest for that Governor and stop. Normal delivery continues to its configured post-publication independent review path.
7. For normal delivery, publish only with the canonical installed `prepush_control.py guarded-push` after its exact-head PASS receipt. Invoke it directly through `/usr/bin/python3`, with no shell wrapper or leading environment assignment, and pass the active owning issue even when a reviewed transfer retains an older surface branch. For direct harness source, proceed only after `APPROVE_SOURCE_HEAD`, use a registered non-SQLite source guard when one is available, and otherwise require an exact current continuation that explicitly authorizes one non-force remote/ref/head update and preserves its before/after/readback publication receipt. A local-authoring packet without that stage stops before publication. Raw or force push as standing authority, hook bypass, alternate refs, no-op commits, and unchanged-head CI reruns are prohibited.
8. Open or update the bounded draft PR only when the selected admission or direct packet stage permits it, preserve the exact issue contract, and wait for natural CI and the configured PR review path. For normal delivery, commission the independent exact-head reviewer here. For direct harness source, require its earlier Governor receipt to remain exact. Resolve material findings and threads through at most one bounded same-scope repair across both review phases; every changed head reruns the complete gates and receives fresh independent acceptance before publication.
9. For normal delivery, merge only when the admission authorizes routine merge. A direct harness writer never merges its own candidate: after terminal-green exact-head CI, independent exact-head acceptance, resolved material threads, current evidence, and no newer control, return the accepted exact head so the Product Planner can invoke the packet's registered match-head mechanism.
10. Close only through the selected route. Normal delivery uses `prepare-terminal-closeout`, the SQLite outbox readback, and `commit-terminal-closeout` before releasing its lease and allocation. Direct harness source never invokes those APIs: after an authorized match-head merge, exact-main green checks, and authorized cleanup, publish and read back the packet-bound GitHub source terminal receipt, return the exact direct-capacity disposition to the Planner, and report `SOURCE_COMPLETE` without claiming installation or activation. If the current packet ends at a validated local head, return its exact validation manifest for a fresh independent Skill Governor and stop before publication.

A successful local gate, guarded publication, PR update, or draft-safe review is intermediate progress, not completion. Continue only while the selected normal admission and terminal watch or direct packet stage remains exact and no hard stop exists.

## Keep native and sandbox permissions separate

The fresh Development writer is the accountable native executor. It performs authorized native Git, GitHub, package/network, applicable Docker/Compose, and cleanup operations under the selected exact admission or direct packet and shared guards.

When authority-bound Git metadata or guarded GitHub/network publication crosses the `workspace-write` sandbox boundary, request one narrowly scoped Auto-review escalation for that exact command. The first protected-path or sandbox-network denial is a capability signal, not by itself a delivery `HOLD`. The escalation grants no new authority: remain fenced to the exact admission or packet, branch, worktree, paths, and publication guard. Raw or force push as standing authority, canonical-checkout edits, out-of-scope paths, unrelated worktrees, broad sandbox/network bypasses, and unchanged retries remain prohibited.

Sandbox children may inspect and patch only their exact owner-local workdir and run deterministic offline checks. They must not claim coordination rows, use Docker or container runners, access GitHub or providers, run outbound Git, download packages or artifacts, or perform cleanup. When useful, require a child to return a literal bounded native-host packet; independently verify it before execution. Sandbox failure never diagnoses native network, credentials, Docker, or provider state.

## Stop at material or operational boundaries

Stop the affected mutation and return a concise proposal to the Product Planner when work requires:

- application/domain behavior or user-visible UX beyond the admitted contract;
- persistent schema/data semantics, public/generated contracts, or authorization/privacy/security/tenant-isolation changes not already exactly authorized;
- a destructive or difficult-to-recover action;
- deployment, hosted provider, cloud, IAM, secret, billing, access, traffic, production, or release work;
- customer/private data or an external commitment;
- a changed diagnosis or scope, out-of-lease repair, new collision, missing evidence, or non-converging repair; or
- any direct user decision.

Only the Product Planner asks the user for approval and creates a fresh or superseding admission or direct harness packet. Continue safe read-only analysis and preserve the current lineage, but do not solicit approval directly, retry unchanged work, or convert an SRE boundary into Development scope.

## Close the attempt

Return evidence appropriate to the exact packet or admission terminal boundary. A local-authoring direct packet returns the committed head and validation manifest, not a fabricated PR or source-terminal receipt. A final owning-issue receipt must bind the issue and PR, exact public head, delivered scenarios, focused and repository-applicable final-head gate evidence, guarded publication, natural CI, independent review, repair disposition, merge or safe HOLD state, cleanup categories and verdict, route-appropriate capacity/lease disposition, residual risks, and next Planner action. Keep exhaustive local paths and hashes owner-local and publish only the validated GitHub-safe projection. Normal delivery requires exact publication readback and `commit-terminal-closeout`; direct harness source requires its packet-bound GitHub readback and never claims a SQLite release.

Do not update #44, #61, #120, #131, or #179 unless an exact narrower Planner instruction names that mutation. End normal delivery after terminal receipt and capacity release, and end direct harness work at the exact packet terminal boundary, such as a local head ready for fresh Governor review. A durable `HOLD` preserves evidence and names the exact blocker. Later work requires a fresh current endpoint attempt or exact direct-packet continuation for the same accepted head.
